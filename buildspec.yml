# buildspec.yml
version: 0.2

phases:

  pre_build:
    commands:
      - cd /var/www/html/

  build:
    commands:
      - echo Build started on `date`
  post_build:
    commands:
      - echo Build completed on `date`
      
artifacts:
  files:
    - '**/*'

