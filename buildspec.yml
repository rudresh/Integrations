# buildspec.yml
version: 0.2

phases:

  build:
    commands:
      - echo Build started on `date`
      - cd /var/www/html/
      
  post_build:
    commands:
      - echo Build completed on `date`
      
artifacts:
  files:
    - '**/*'

